{
  "name": "supermarket-sales-proxy",
  "version": "0.1.0",
  "description": "Proxy server to forward chat requests to OpenAI (keeps API key on server)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
